spring:
  datasource:
    username: container-therapy-backend
    password: container-therapy-backend
    url: jdbc:mysql://localhost:3306/container-therapy-backend
    
  jpa:
     hibernate:
        ddl-auto: update
     show-sql: true
     defer-datasource-initialization: true
     
  sql:
     init:
        mode: always